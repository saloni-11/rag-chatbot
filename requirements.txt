# ─── Full requirements.txt (for Docker / deployment) ─
# For LOCAL development, use the phased files instead:
#   pip install -r requirements-phase2.txt   (start here)
#   pip install -r requirements-phase3.txt   (embeddings + vector store)
#   pip install -r requirements-phase4.txt   (RAG + Groq LLM)
#   pip install -r requirements-phase6.txt   (FastAPI backend)

# ─── RAG Framework ───────────────────────────
llama-index-core==0.10.68
llama-index-vector-stores-chroma==0.1.10
llama-index-embeddings-huggingface==0.2.3
llama-index-llms-groq==0.1.4

# ─── Vector Store ────────────────────────────
chromadb==0.5.5

# ─── LLM & Embeddings ────────────────────────
groq==0.9.0
sentence-transformers==3.0.1

# ─── Document Processing ─────────────────────
pypdf==4.3.1
beautifulsoup4==4.12.3

# ─── API ─────────────────────────────────────
fastapi==0.112.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
python-multipart==0.0.9

# ─── Utilities ───────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.5.0

# ─── Evaluation (Phase 10 only) ──────────────
# ragas==0.1.18
# datasets==2.21.0
