[pytest]
# Look for tests in the tests/ directory
testpaths = tests

# Only discover files that match test_*.py
python_files = test_*.py

# Show verbose output + short traceback on failures
addopts = -v --tb=short

# Ignore the frontend directory (has its own node_modules etc.)
norecursedirs = frontend node_modules data .git __pycache__
